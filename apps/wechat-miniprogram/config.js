module.exports = {
  // Local preview: use the development machine's LAN address so a phone can reach it.
  // Replace with the production HTTPS API before upload.
  apiBaseUrl: 'http://10.183.13.28:8002/api/v1',
  devMode: true
}
